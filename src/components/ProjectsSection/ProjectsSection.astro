<!-- ---


   
---

<section id='projects' class='my-10 max-w-screen'>
  <div>
    <h5 class='sm:text-[18px] text-[14px] text-secondary uppercase tracking-wider'>MY WORK</h5>
    <h2 class='text-white font-black md:text-[60px] sm:text-[50px] xs:text-[40px] text-[30px]'>
      Projects.
    </h2>
  </div>

  <div class='my-8'>
    <h4 class='sm:text-[18px] text-[14px] text-secondary uppercase tracking-wider'>
      {t.finishedProyectsTitle}
    </h4>
    <div class='mt-8 flex flex-wrap gap-7'>
      {
        t.projects.map((project, index) => (
          // eslint-disable-next-line react/jsx-no-undef
          <ProjectCard key={index} index={index} {...project} />
        ))
      }
    </div>
    <div class='my-8'>
      <h4 class='sm:text-[18px] text-[14px] text-secondary uppercase tracking-wider'>
        {t.inProgressTitle}
      </h4>
      <div class='mt-8 flex flex-wrap xl:grid xl:grid-cols-3 gap-7'>
        {
          t.inprogress?.map((project, index) => (
            // eslint-disable-next-line react/jsx-no-undef
            <ProjectCard key={index} index={index} {...project} />
          ))
        }
      </div>
    </div>
  </div>
</section>
 -->